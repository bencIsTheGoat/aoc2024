{
	"version": "0.2.0",
	"configurations": [
			{
					"name": "Launch ./cmd/bin",
					"type": "go",
					"request": "launch",
					"mode": "debug",
					"program": "${workspaceFolder}/cmd/bin",
					"cwd": "${workspaceFolder}",
					"env": {}, // Optional: Add environment variables
					"args": [] // Optional: Add command-line arguments
			}
	]
}